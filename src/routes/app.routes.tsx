import { BrowserRouter as Router, Route, Routes, Navigate, redirect } from "react-router-dom";

const AppRoutes = () => {
    return (
        <Router>
            <Routes>
            </Routes>
        </Router>
    )
}